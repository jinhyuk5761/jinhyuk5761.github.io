# 설계 문서 대비 변경점

착수 체크리스트 1번("실응답 캡처 → 실제 필드명 확정")을 먼저 수행한 결과,
설계 문서의 전제 몇 개가 실제 데이터와 달랐다. 전부 **실응답 기준으로 구현**했고,
근거를 아래에 남긴다. 확인 시점은 2026-08-13.

---

## 1. 사용률 category 값이 다르다 — 그리고 `tera` 는 존재하지 않는다

| 설계 문서 | 실응답 |
|---|---|
| `move` | `move` ✅ |
| `item` | **`held_item`** |
| `ability` | `ability` ✅ |
| `nature` | **`stat_alignment`** (`stat_up`/`stat_down` 동반) |
| `spread` | **`stat_points`** (`hp_points` 등 6개 컬럼) |
| `teammate` | `teammate` ✅ |
| `tera` | **없음** |

`tera` 는 로스터 전체(싱글·더블 양쪽)를 훑어도 나오지 않는다.
Pokémon Champions 는 테라스탈이 아니라 **메가진화** 중심 게임이다 — 로스터 236종 중
메가폼이 73종이다. 그래서 테라 관련 UI를 만들지 않았다.

`teammate` 행은 `percentage` 가 빈 문자열, `percentage_value` 가 `null` 이다.
비율을 0으로 위조하지 않고 "비율 미제공"으로 표시한다.

## 2. Smogon 에 **더블 C&C 가 존재한다**

설계 문서 M3: "Smogon 에 더블 C&C 가 없으므로 싱글 BSS 데이터를 표시하고 '싱글 기준' 배지 명시".

실제로는 `gen9championsvgc2026regmb` 에 완전한 Checks & Counters 블록이 있다
(2026-07 기준 176만 배틀, 302종). 따라서 **싱글 대체 규칙을 적용하지 않고
각 포맷의 실데이터를 쓴다.** Showdown ≠ Switch 경고는 설계대로 상시 노출한다.

| 포맷 | 사용 메타게임 |
|---|---|
| 싱글 | `gen9championsbssregmb` |
| 더블 | `gen9championsvgc2026regmb` |

## 3. 규정(Reg) 코드가 문서보다 진행됐다

문서는 `regma` / `regmb` 를 병기했으나, 2026-07 시점 현행은 **`regmb`** 이고
`regma` 는 2026-06 이전이다. 빌드 스크립트는 최신 규정을 자동 선택하고,
**규정이 다른 달은 합산에서 제외한다** — 룰이 다르면 메타도 다르므로 섞으면 통계가 오염된다.

## 4. API 의 수치는 종족값이 아니라 '실수치'다

**초기 구현에서 이 값을 종족값이라고 라벨링했는데 틀렸다.** 처음에는 "Champions 가
자체 밸런스 스케일을 쓴다"고 판단했으나, 그게 아니라 단순히 **다른 종류의 수치**였다.

championsbattledata 의 `hp`/`attack`/... 은 **레벨 50 · 개체값 31 · 노력치 0 ·
무보정 성격**일 때의 실수치다. 본가 공식을 그대로 따른다:

```
HP   = floor((2*종족값 + 개체값 + 노력치/4) * 레벨/100) + 레벨 + 10
그외 = floor((2*종족값 + 개체값 + 노력치/4) * 레벨/100) + 5
```

레벨 50 · 개체값 31 · 노력치 0 을 넣으면 floor 가 깔끔히 떨어져 상수 덧셈이 된다:

| | 관계 | 예 (한카리아스) |
|---|---|---|
| HP | 실수치 = 종족값 + 75 | 108 → 183 |
| 그 외 | 실수치 = 종족값 + 20 | 공격 130 → 150 |

로스터 표본 8종으로 검증했고 전부 일치했다. 합계도 맞는다 —
한카리아스 실수치 합 775 ↔ 본가 종족값 합 600, 알로라 나인테일 680 ↔ 505.

역환산이 손실 없이 정확하므로 화면에는 **실수치와 종족값을 함께** 보여준다.
`src/core/stats.ts` 에 공식과 근거를 적어두고 `test/stats.test.ts` 로 고정했다.

이 정정과 별개로 **PokéAPI 수치는 여전히 쓰지 않는다.** PokéAPI 는 종족값을 주는데
Champions 는 폼별 실수치를 주므로, 섞으면 단위가 다른 값이 한 화면에 나온다.
PokéAPI 는 **한/일 명칭 전용**이고, 수치·타입·스프라이트의 유일한 출처는
championsbattledata 다 (인덱스의 `summary.primary` 에 전부 들어 있어 추가 호출도 없다).

## 5. Showdown 은 learnset 이 아니라 '기술 플래그' 때문에 필요하다

championsbattledata 인덱스가 종별 `learnableMoveNames` 를 동봉하므로 learnset 은
따로 받지 않는다. 그런데 Showdown 이 다른 이유로 필요해졌다.

**PokéAPI 는 기술 플래그를 제공하지 않는다.** move 응답에 `flags` 필드 자체가 없다.
그런데 철주먹(펀치)·단단한발톱(접촉)·옹골찬턱(깨물기)·메가런처(파동)·방탄(구슬)·
예리함(베기)·펑크록(소리)·복슬복슬(접촉)·방진(가루) 같은 특성이 전부 이 플래그에 걸린다.
플래그가 없으면 대미지 계산에서 이 특성들을 반영할 수 없다.

Showdown 소스(`data/moves.ts`, MIT)에서 플래그만 추출해 기술 데이터에 붙인다.
소스 파일을 재배포하지 않고 필요한 플래그만 뽑는다.

추출 결과(로스터 539기술 기준): contact 181 · sound 25 · slicing 19 · bullet 17 ·
punch 16 · wind 9 · bite 7 · pulse 6 · powder 6.

덕분에 대미지 관여 특성 커버리지가 로스터 200종 중 **45종**이 됐고,
미지원은 스킬링크 하나만 남았다(연속기 타격 수 모델링이 먼저 필요하다).

## 6-1. 사용률 순위는 인덱스가 이미 들고 있다

`summary.battleSummary.Current.<포맷>.position` 이 그 종의 사용률 순위다.
싱글·더블이 따로 있고, 235종에 1~235 가 중복 없이 붙어 있다 (rotomfan 만 없다).

즉 순위를 위해 종별 배틀 데이터를 236번 받을 필요가 없다. 앱이 이미 하는 `/api` 요청
하나에 다 들어 있어서, 재배포 없이도 상류가 갱신되면 순위가 따라온다.

주의: 상류에 `rotomfan` 이 2건 있는데 **한쪽에만** position 이 붙어 있다.
중복 제거를 순서에만 맡기면 순위를 통째로 잃는다 — 순위가 있는 쪽을 남긴다.

## 6. 사용률은 종 단위, 카운터는 폼 단위 (비대칭)

배틀 API 는 `showdownId`(종) 로 라우팅되므로 Mega Garchomp 와 Garchomp 의 사용률이
같은 응답에 들어온다. 반면 Smogon 은 `Garchomp` 와 `Garchomp-Mega` 를 별개로 집계한다.

이 비대칭을 UI 에서 숨기지 않는다:
- 사용률 탭에는 "사용률은 폼 구분 없이 종 단위로 집계됩니다" 라고 적는다.
- 카운터 탭은 폼별 섹션으로 나눠 보여준다.

## 7. 상류 데이터에 `showdownId` 중복이 있다

`rotomfan` 이 "Fan Rotom" / "Rotom Fan" 두 엔트리로 들어온다.
어댑터에서 중복을 제거하되 **순위(position)가 붙은 쪽을 남긴다** — 한쪽에만 순위가 있어서
선착순으로 버리면 순위를 통째로 잃는다 (6-1 참고). 테스트로 고정했다.

## 8. M5 는 자동 수집이 불가능하다

`champs.pokedb.tokyo` 와 구축 기사 호스트들이 자동 요청에 **403** 을 돌려준다.
차단을 우회하는 대신 **사람이 채우는 큐레이션 입력**(`data/frozen-season.source.json`)
방식으로 만들었다. 설계 문서 6절의 규칙(출처 필수, X/YouTube 제외)은 서버가 요청 시점에
강제하고, 입력이 비면 M5 화면은 안내문으로 degrade 한다.

**현재 데이터는 비어 있다.** 파일에 항목을 적으면 재시작 없이 반영된다(9절 참고).

## 9. 기능 가용 여부를 빌드타임이 아니라 런타임이 결정한다

초기 구현은 `VITE_ENABLE_RANKING` 같은 빌드타임 flag 로 기능을 켰다. 그러면
**데이터가 생겨도 재빌드·재배포를 해야 화면에 붙는다.** 요구사항("데이터가 생기면 실시간으로")과
맞지 않아 런타임 감지로 바꿨다.

- 서버가 `/api/config` 로 "무엇에 데이터가 있는지"를 알린다.
- 클라이언트는 60초마다 확인하고, 버전이 바뀌면 화면을 다시 그린다.
- 데이터 URL 에 버전이 실려 있어 캐시가 자연히 비켜간다 (별도 퍼지 로직 없음).

반영 경로 세 가지:

| 대상 | 감지 방식 | 재시작 |
|---|---|---|
| M5 구축 | 큐레이션 파일 mtime | 불필요 |
| Smogon 카운터 | 6시간 주기로 새 달·새 규정 확인 후 재생성 | 불필요 |
| M6 랭킹 | 환경변수 | 필요 |

재생성 산출물은 `var/data/` 에 쓴다. `dist/` 를 건드리면 다음 배포가 조용히 덮어쓰기 때문이다.

## 10. M6 공식 랭킹 엔드포인트를 확보하지 못했다

`ranking.app.pokemonchampions.jp` 는 인증 없는 요청에 **403** 을 돌려준다
(`api.app.pokemonchampions.jp` 루트는 200 이지만 12바이트 스텁).

동작하는 공개 경로를 알아내지 못했으므로 URL 을 코드에 박지 않고
`CHAMPIONS_RANKING_URL` 환경변수로 주입하게 했다. 기본값은 **꺼짐**이고,
꺼진 상태에서 M1~M5 가 완전히 동작하는 것을 테스트로 고정했다.
어댑터·서버 캐시·백오프·정규화·UI 는 전부 구현·검증돼 있다
(가짜 상류 서버로 end-to-end 확인).

## 11. 기술 정보는 나무위키가 아니라 PokéAPI 에서 받는다

기술 한국어명·설명·제원의 출처로 나무위키 요청이 있었으나 PokéAPI 로 갔다. 이유 셋:

1. **라이선스.** 나무위키 문서는 CC BY-NC-SA 2.0 KR 이다. 본문을 앱에 실으면
   앱 전체가 비영리·동일조건 라이선스에 묶인다.
2. **접근.** 자동 요청을 차단한다. 다른 소스와 같은 원칙으로 우회하지 않는다.
3. **정확도.** PokéAPI 는 **게임 내 공식 한국어 설명**과 위력/PP/명중률/분류/우선도를
   구조화해서 준다. 이 용도에는 사람이 쓴 산문보다 이쪽이 맞다.

결과: 로스터가 배우는 기술 539개 전부 한국어명 확보, 그중 485개는 공식 한국어 설명.
나머지 54개는 9세대 신규 기술이라 PokéAPI 의 한국어 설명이 소드/실드까지만 있어
영문으로 폴백하고 화면에 그 사실을 밝힌다.

같은 파이프라인으로 타입 18 · 도구 141 · 특성 200 · 성격 23 도 한국어로 받았다
(전부 매칭, 미매칭 0). 도구·성격 목록은 배틀 데이터의 held_item / stat_alignment 에서
수집한다 — PokéAPI 전체 도구 2천여 개 중 대전에 나오는 건 일부뿐이다.

### 모호한 표현을 숫자로

공식 게임 텍스트는 "공격을 크게 올린다" / "때때로 마비시킨다" 처럼 정도를 말로 쓴다.
PokéAPI 는 같은 내용을 숫자로 갖고 있어(`stat_changes`, `ailment_chance`, `meta.*`)
`src/core/moveEffect.ts` 가 이를 "공격 +2랭크" / "10% 확률로 마비" 로 바꿔 설명 위에 얹는다.
공식 설명은 지우지 않는다 — 대체가 아니라 보강이다.

**한계:** 구조화되지 않은 정보가 있다. 배북의 +6랭크, 역린의 지속 턴은 PokéAPI 에
비어 있다. 그런 항목은 정밀 효과 줄을 아예 만들지 않고 공식 설명 문장에 맡긴다.
없는 값을 추측해 채우지 않는다.

## 12. 표시 언어는 한국어 단일

설계 문서 M1 은 로케일 명칭 보강(한/일/영)을 요구했다. 화면 표시는 **한국어만** 쓰기로 했고,
일본어·영어 명칭은 **검색 대상으로는 그대로 유지**한다 —
"한카리아스 / garchomp / ガブリアス 가 같은 결과"라는 M1 완료 기준이 여전히 성립해야 하기 때문.
언어 선택 UI 는 제거했다.

## 13. 도구 목록은 Champions 에 실재하는 141종에서만 고른다

배틀 데이터의 `held_item` 집계에 실제로 잡히는 도구가 141종이다. 본가에 있어도
Champions 에 없는 도구(구애머리띠·구애안경·돌격조끼·진화의휘석)는 목록에 넣지 않는다.
없는 도구를 두면 성립하지 않는 조합의 대미지를 계산하게 된다. 구애 계열 중에는
**구애스카프만** 존재한다.

한국어 표기는 코드에 적지 않고 `terms.json` 에서 가져온다. 손으로 옮겨 적었다가
'달인의띠'를 '전문가벨트'로 쓴 적이 있다. `test/items.test.ts` 가 코드에 한글 도구명이
남으면 실패시킨다.

PokéAPI 가 한국어를 안 주는 도구는 빌드 스크립트의 override 로만 채운다
(요정의깃털, Champions 오리지널 메가스톤 34종). 추측으로 채우지 않는다.

## 14. 메가 폼은 지닌 도구가 메가스톤으로 고정된다

메가진화는 해당 메가스톤을 들어야만 성립하므로, 메가 폼을 고르면 도구 선택이 잠긴다.
생명의구슬을 든 메가 한카리아스는 존재할 수 없다.

상류에 "이 폼은 이 돌" 이라는 필드가 없어서 이름으로 잇는다 — 돌 이름의 앞부분이
종/폼 이름과 가장 길게 겹치는 것을 고른다. 추측이 섞이는 방식이라
`test/megaStones.test.ts` 가 로스터의 메가 폼 79개를 전수로 확인한다.

주의할 점 둘: `form_kind` 는 `Mega` 말고 `Mega X`·`Mega Y` 로도 온다.
그리고 지역폼 종(Galarian Slowbro)은 종 이름이 돌 이름과 한 글자도 안 겹쳐서
폼 이름(`Mega Slowbro`)으로 맞춰야 한다.

## 15. 광역기는 두 갈래로 나눠 표기한다

`all-opponents`(상대 전체)와 `all-other-pokemon`(자신 제외 전원 — 아군도 맞는다)은
더블에서 쓰임이 완전히 다르다. 둘 다 대미지가 0.75배가 되지만 하나로 뭉뚱그리면
지진과 눈보라를 같은 기술처럼 읽게 된다.

접촉 여부는 **없을 때도** 적는다. "표기가 없는 것"과 "비접촉인 것"이 구별돼야
까칠한피부·불꽃몸 같은 방어측 특성 판정을 읽을 수 있다.

## 16. 카운터 위 Smogon 고지를 걷어냈다

설계 문서 6절은 Smogon 데이터를 보여주는 모든 자리에 출처 고지를 상시 노출하라고
적었다. 앱에서는 카운터 탭을 열 때마다 표보다 고지가 먼저 보여 정작 볼 것이 밀렸다.
고지 자체를 없앤 것이 아니라 자리를 옮긴 것이다 — 출처는 접근성 탭에 그대로 있다.

## 17. 드롭다운은 자기가 고른 값을 자기가 표시한다

`searchSelect` 가 선택값을 내부에 들고 있다가 직접 다시 칠한다.

부모에게 맡겼더니 도구를 골라도 버튼에 '없음' 이 남았다. 계산은 정상이었는데
화면만 안 바뀌어서 "아이템이 안 들어간다" 로 보였다. 계산기는 대부분의 입력에서
결과만 다시 그리기 때문에(부분 렌더링) 부모가 버튼을 다시 만들어 주지 않는다.

같은 이유로 상세 탭 전환도 헤더까지 지우지 않고 패널만 갈아 끼운다. 통째로 지우면
화면이 깜빡이고, 높이가 0이 되는 순간 스크롤이 맨 위로 튄다.

## 18. 상태이상은 화상만, 특성은 「없음」 없이

**상태이상**: 대미지 수치가 달라지는 것은 화상(물리 ×0.5)뿐이다. 나머지는
"상태이상이기만 하면" 켜지는 판정(객기·이상한비늘·근성)이라 화상으로 똑같이 걸린다.
독을 따로 보는 베놈쇼크·독침천발은 Champions 기술 목록에 없어 고를 이유가 없다.
목록을 일곱 줄 두고 여섯 줄이 결과를 안 바꾸면 "골라도 소용없네" 로 읽힌다.

**특성**: 특성 없는 포켓몬은 대전에 나오지 않는다. '없음' 을 고를 수 있게 두면
실재하지 않는 조합을 계산하게 된다. 안 고른 상태면 사용률이 많은 특성으로 잡고,
그것이 없으면 도감의 첫 특성으로 맞춘다.

## 19. 드롭다운은 화면 최상단에 띄운다

목록이 열리면 검색창에 포커스가 가고 키보드가 화면 아래를 덮는다. 버튼 아래에 편
패널은 그대로 가려진다. 위로 펴는 것도 버튼이 화면 위쪽에 있으면 자리가 안 난다.

그래서 버튼 위치와 무관하게 `position: fixed` 로 화면 맨 위에 붙인다. 가로는 버튼에
맞추되 화면 밖으로 나가면 밀어 넣고, 좁은 칸(랭크)에서는 220px 까지 넓힌다.
높이는 `visualViewport` 로 잰다 — 키보드가 뜨면 이 값이 줄어들어서 목록이 키보드
밑으로 흘러 들어가지 않는다.

`position: fixed` 는 조상에 `transform`·`filter`·`contain` 이 있으면 그 조상 기준이
된다. 지금 스타일시트에는 셋 다 없다. 넣게 되면 패널을 `document.body` 로 옮겨야 한다.
